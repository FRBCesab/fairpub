% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fp_identify_duplicate_works.R
\name{fp_identify_duplicate_works}
\alias{fp_identify_duplicate_works}
\title{Identify duplicate works based on title similarity}
\usage{
fp_identify_duplicate_works(
  data,
  string_dist = "lv",
  hclust_method = "single",
  threshold = 0.2
)
}
\arguments{
\item{data}{a \code{data.frame} containing at least a \code{title} column.}

\item{string_dist}{a \code{character} string specifying the distance
metric used by \code{\link[stringdist:stringdistmatrix]{stringdist::stringdistmatrix()}}. Defaults to \code{"lv"}
(Levenshtein distance).}

\item{hclust_method}{a \code{character} string specifying the hierarchical
clustering method used by \code{\link[stats:hclust]{stats::hclust()}}.
Defaults to \code{"single"}.}

\item{threshold}{a \code{numeric} value controlling cluster separation.
Lower values produce more fine-grained clusters (stricter matching),
while higher values merge more records into the same group.}
}
\value{
The input \code{data.frame} with an additional column:
\describe{
\item{ref_id}{Integer cluster identifier grouping similar titles.}
}
}
\description{
Groups potentially duplicate bibliographic records by computing
pairwise string distances between work titles and clustering
similar items.
}
\details{
Title similarity is computed after basic text normalization
(lowercasing, punctuation removal, whitespace trimming).
Distances are calculated using \code{\link[stringdist:stringdistmatrix]{stringdist::stringdistmatrix()}} and
normalized by title length before hierarchical clustering.

This function does not remove duplicates but assigns a cluster
identifier that can be used for downstream deduplication or grouping.
}
\examples{
\dontrun{
df <- data.frame(
  title = c(
    "Deep Learning for NLP",
    "Deep learning for natural language processing",
    "Quantum Computing Basics"
  )
)

fp_identify_duplicate_works(df)
}
}
